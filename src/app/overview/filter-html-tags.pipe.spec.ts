import { FilterHtmlTagsPipe } from './filter-html-tags.pipe';

describe('FilterHtmlTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHtmlTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
